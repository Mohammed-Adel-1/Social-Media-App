

export const multer_enum = {
    image: ["image/png", "image/jpg", "image/jpeg"],
    video: ["video/mp4"],
    pdf: ["application/pdf"]
}

export enum store_enum {
    disk = "disk",
    memory = "memory"
}